name: AWS Credentials
on:
  pull_request
permissions:
  id-token: write
  contents: read
# defaults:
#   run:
#     shell: bash
#     working-directory: terraform
jobs:
  aws_credentials:
    runs-on: ubuntu-latest
    steps:
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v4.1.0
        with:
          audience: sts.amazonaws.com
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::462753922172:role/GitHub

      - name: setup terraform
        uses: hashicorp/setup-terraform@v3

      - name: terraform fmt
        id: fmt
        run: terraform fmt -check -chdir=./terraform

      - name: terraform init
        id: init
        run: terraform init -input=false -chdir=./terraform

      - name: terraform validate
        id: validate
        run: terraform validate -no-color -chdir=./terraform

      - name: terraform plan
        id: plan
        run: terraform plan -no-color -input=false -chdir=./terraform
